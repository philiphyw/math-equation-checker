import { EquationValidators } from './equation-validators';

describe('EquationValidators', () => {
  it('should create an instance', () => {
    expect(new EquationValidators()).toBeTruthy();
  });
});
